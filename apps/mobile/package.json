{
  "name": "@ticket-platform/app-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Buyer mobile app skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for app-mobile'",
    "lint": "echo 'TODO: add lint for app-mobile'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for app-mobile'"
  }
}
