# Proxy mode (recommended in dev)
# Keep as /v1 when using Vite proxy to local api-gateway
VITE_API_BASE_URL=/v1

# Demo identities for local backend
VITE_BUYER_USER_ID=user_demo_001
VITE_ORGANIZER_ID=org_rockfest
VITE_STAFF_GATE_ID=gate-a

# Local-only development key used for purchase confirm shortcut
VITE_INTERNAL_API_KEY=internal-dev-key
