{
  "name": "@ticket-platform/svc-event-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Event service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-event-service'",
    "lint": "echo 'TODO: add lint for svc-event-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-event-service'"
  }
}
