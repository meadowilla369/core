{
  "name": "@ticket-platform/svc-refund-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Refund service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-refund-service'",
    "lint": "echo 'TODO: add lint for svc-refund-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-refund-service'"
  }
}
