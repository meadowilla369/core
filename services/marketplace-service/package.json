{
  "name": "@ticket-platform/svc-marketplace-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Marketplace service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-marketplace-service'",
    "lint": "echo 'TODO: add lint for svc-marketplace-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-marketplace-service'"
  }
}
