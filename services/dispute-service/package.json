{
  "name": "@ticket-platform/svc-dispute-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Dispute service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-dispute-service'",
    "lint": "echo 'TODO: add lint for svc-dispute-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-dispute-service'"
  }
}
