{
  "name": "@ticket-platform/svc-recovery-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Recovery service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-recovery-service'",
    "lint": "echo 'TODO: add lint for svc-recovery-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-recovery-service'"
  }
}
