{
  "name": "@ticket-platform/svc-notification-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Notification service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-notification-service'",
    "lint": "echo 'TODO: add lint for svc-notification-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-notification-service'"
  }
}
