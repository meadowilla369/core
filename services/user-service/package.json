{
  "name": "@ticket-platform/svc-user-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "User service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-user-service'",
    "lint": "echo 'TODO: add lint for svc-user-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-user-service'"
  }
}
