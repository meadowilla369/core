{
  "name": "@ticket-platform/svc-kyc-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "KYC service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-kyc-service'",
    "lint": "echo 'TODO: add lint for svc-kyc-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-kyc-service'"
  }
}
