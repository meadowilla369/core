{
  "name": "@ticket-platform/svc-kyc-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "KYC service runnable skeleton",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.888.0",
    "@ticket-platform/local-infra": "workspace:*",
    "pg": "^8.16.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js",
    "test": "echo 'TODO: add tests for svc-kyc-service'",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier src --check"
  }
}
