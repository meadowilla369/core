{
  "name": "@ticket-platform/svc-api-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "API gateway skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-api-gateway'",
    "lint": "echo 'TODO: add lint for svc-api-gateway'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-api-gateway'"
  }
}
