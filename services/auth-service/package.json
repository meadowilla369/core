{
  "name": "@ticket-platform/svc-auth-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Auth service skeleton",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo 'TODO: add tests for svc-auth-service'",
    "lint": "echo 'TODO: add lint for svc-auth-service'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for svc-auth-service'"
  }
}
