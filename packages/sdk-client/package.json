{
  "name": "@ticket-platform/sdk-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo 'TODO: add tests for sdk-client'",
    "lint": "echo 'TODO: add lint for sdk-client'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'TODO: add formatter for sdk-client'"
  }
}
